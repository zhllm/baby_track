import 'package:flutter/cupertino.dart';

class TinyMediaPage extends StatelessWidget {
  const TinyMediaPage({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container(
      child: Center(
        child: Text('TinyMediaPage'),
      ),
    );
  }
}
